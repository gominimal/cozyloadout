# bat — {{scheme-name}}.
#
# GENERATED by `just theme` — see README.md.
#
# The theme itself lives at ~/.config/bat/themes/{{scheme-slug}}.tmTheme. bat
# only sees it once its cache has been rebuilt:
#
#     just bat-cache
#
# `bat --list-themes` should then show {{scheme-slug}}.
--theme="{{scheme-slug}}"

# The theme italicises comments, matching the helix theme. bat suppresses
# italics unless asked, so ask.
--italic-text=always
