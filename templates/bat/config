# bat — {{scheme_name}}.
#
# GENERATED by `just theme` — see AGENTS.md.
#
# The theme itself lives at ~/.config/bat/themes/{{scheme_slug}}.tmTheme. bat
# only sees it once its cache has been rebuilt, which the lifecycle hooks do
# when the session comes up. By hand it is:
#
#     bat cache --build
#
# `bat --list-themes` should then show {{scheme_slug}}.
--theme="{{scheme_slug}}"

# The theme italicises comments, matching the helix theme. bat suppresses
# italics unless asked, so ask.
--italic-text=always
